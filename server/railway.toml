[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run db:generate && npx prisma migrate deploy"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
